# Core Django and dependencies
Django>=6.0,<7.0
python-dotenv==1.2.1
whitenoise==6.11.0

# Custom User Model - Timezone support
django-timezone-field==7.2.1
pytz==2025.2

# Field encryption
# Using native cryptography library (actively maintained, v46.0.3)
# Custom EncryptedTextField in apps/journal/fields.py
cryptography==46.0.3

# Tagging system
django-taggit==6.1.0

# Authentication
django-allauth==65.13.1

# Rate limiting / Brute force protection
django-axes==8.1.0

# Content Security Policy
django-csp==4.0

# Image processing (for avatars)
Pillow>=11.0.0

# Database
psycopg2-binary==2.9.11  # PostgreSQL adapter (for production)
